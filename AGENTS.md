# OmniPal — AGENTS.md

本文档是给编码代理（AI Agent）与人类开发者的强制约束文件。  
所有代码生成、修改、重构必须遵守本文档。与主开发文档冲突时，以本文档的安全与范围边界为准。

**项目版本对应**：OmniPal v0.4（架构修正版）  
**最后更新**：2026-09-13

---

## 1. 项目本质（必须牢记）

OmniPal 是一个**纯运行时内存热切换**的肌肉记忆引擎，叠加必要的轻量界面。

- 核心是内存中的绑定 Overlay，而不是配置文件生成器。
- 默认零配置污染，退出必须能完整恢复。
- 目标是比 Omapala 更干净、更轻，而不是把 Omapala 的复杂系统再实现一遍。

**禁止把项目做成**：
- 重型配置管理系统
- 多职责庞大 Engine
- 默认写 Hyprland 配置文件的工具
- Omapala 的增强复刻版

---

## 2. 架构铁律

1. **绑定修改权唯一**  
   只有 Engine 可以修改 Hyprland 绑定。插件禁止直接调用 `hyprctl keyword bind/unbind` 或写配置文件。

2. **默认零文件写入**  
   正常切换模式不得写入 `~/.config/hypr/` 下的任何文件。持久化必须是用户显式开启的可选功能。

3. **单一事实源**  
   所有快捷键与动作定义只存在于 `schema/actions.json` 与 `profiles/*`。  
   QML、文档、测试中禁止硬编码键位列表。

4. **退出必须可恢复**  
   Engine 收到 SIGTERM / SIGINT / 正常退出时，必须执行完整 Restore，把绑定恢复到启动前的状态。

5. **插件只做展示与触发**  
   插件通过 IPC 或 CLI 查询状态、发送切换命令，不实现绑定逻辑，不维护自己的键位副本。

---

## 3. 技术边界与禁止事项（Hard Stop）

以下事项**严禁实现或调研绕过**，除非用户明确要求并批准边界变更：

| 编号 | 禁止项 | 说明 |
|------|--------|------|
| H-1 | 默认写入用户 Hyprland 配置 | 不得自动修改 `bindings.lua` 或其他主配置 |
| H-2 | 应用内快捷键拦截 | 不得重映射 Ctrl+C/V/Z 等应用快捷键 |
| H-3 | 全局输入重映射依赖 | 不得把 keyd、kanata、ydotool 等作为默认依赖 |
| H-4 | 实时鼠标拖拽吸附 | 只允许键盘触发的吸附 |
| H-5 | 窗口像素缩略图抓取 | Dock / Overview 只使用元数据（图标、标题、状态） |
| H-6 | 修改 `/usr/share/omarchy/` | 只读参考，严禁写入 |
| H-7 | 自动修改 `shell.json` | 不得静默改动用户 bar 布局 |
| H-8 | 重型常驻复杂守护逻辑 | Engine 保持职责极简，禁止塞入过多功能 |

**允许的替代方案**：
- 快捷键覆盖仅限 Compositor 级别动作
- 吸附仅通过键盘 + 可选视觉反馈
- 需要持久化时，必须做成明确的可开关选项，并提供干净卸载

---

## 4. 开发范围控制

### 4.1 当前优先实现（按阶段）

**Phase 0（必须先完成）**
- 内存热切换（windows / mac / omarchy）
- 自动 Restore
- 基础 CLI：`switch` / `cycle` / `restore` / `status`
- 干净安装与卸载脚本

**Phase 1**
- `omni.mode-indicator`
- `omni.cheat-sheet`
- 简单 `omni.settings`
- 状态广播与优雅降级

**Phase 2 及以后**
- 基础 Snap + snap-feedback
- mac-dock、overview
- 可选持久化、自定义 Profile

### 4.2 明确延后或默认不做

- 复杂 Window Mode 动态打标系统
- 重型冲突策略引擎
- 使用习惯追踪
- 完整配置事务编译器
- 大型设置中心

新增功能前，必须先确认它不会破坏“轻量、零污染、可恢复”的核心原则。

---

## 5. 代码与工程约定

### 5.1 目录与职责

- `engine/`：唯一负责绑定切换与状态的核心
- `plugins/`：只负责 UI，每个插件可独立安装
- `schema/` + `profiles/`：唯一数据源
- `scripts/`：安装、卸载、一致性检查

### 5.2 插件规范

- 插件 ID 统一使用 `omni.<name>` 形式（如 `omni.mode-indicator`）
- 必须包含合法 `manifest.json`
- 无 Engine 时必须优雅降级，禁止抛出导致 Shell 崩溃的异常
- 禁止依赖 monorepo 绝对路径

### 5.3 数据与测试

- 任何键位或动作变更，必须先改 `schema/actions.json` 或对应 Profile
- 提交前应运行一致性检查（如有 `check-consistency` 脚本）
- 禁止在 QML 中维护独立的快捷键列表副本

### 5.4 风格

- 保持代码简单直接，优先可读性与可删除性
- 不引入不必要的抽象和设计模式
- 注释只解释非显而易见的意图，不写废话

---

## 6. 安全与卸载承诺

1. 安装不得破坏用户现有快捷键配置。
2. 卸载必须能把系统恢复到安装前状态。
3. 不得留下残留的 bind、后台进程或配置片段。
4. 不收集键盘内容，不做 keylogging。
5. 不要求 root 权限。

---

## 7. 给编码代理的执行规则

1. **先读本文档和主开发文档，再写代码。**
2. 如果需求会引入配置写入、重型守护进程、全局输入拦截或大幅增加复杂度，先停止并说明风险。
3. 优先实现可运行的最小路径，再考虑扩展。
4. 修改绑定相关逻辑时，必须同时考虑 Restore 路径。
5. 不要为了“功能完整”而把 Omapala 的复杂设计重新加回来。
6. 当存在多种实现方式时，选择更简单、更少文件写入、更容易卸载的方案。

---

## 8. 决策优先级（冲突时按此顺序）

1. 安全与可恢复性
2. 零污染与轻量
3. 切换速度与无闪烁
4. 用户真正需要的界面完整性
5. 功能数量

---

## 9. 一句话红线

> 宁可少做功能，也不要再做成一个沉重、写配置、难卸载、状态不一致的系统。

---

**文档结束**

本文件应随架构重要变更同步更新。若需扩大安全边界或增加默认持久化等行为，必须先明确修改本文件对应条款。
